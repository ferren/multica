Index: multica/server/pkg/taskfailure/classify.go
===================================================================
--- multica.orig/server/pkg/taskfailure/classify.go
+++ multica/server/pkg/taskfailure/classify.go
@@ -133,6 +133,13 @@ func Classify(rawError string) Reason {
 			// would not match the curly form either, so this is a small
 			// in-flight improvement on top of the SQL classifier.
 			"you\u2019ve hit your limit",
+			// Claude Code's plan/session cap surfaces as
+			// "You've hit your session limit \u00b7 resets 8pm (Asia/Shanghai)".
+			// The generic "you've hit your limit" rule does NOT match it
+			// ("session" sits between "your" and "limit"), so it has its
+			// own entry. Treated as a quota limit so downstream can parse
+			// the reset time and resume (issue #157).
+			"session limit",
 			"credits",
 			"quota",
 		):
Index: multica/server/pkg/taskfailure/classify_test.go
===================================================================
--- multica.orig/server/pkg/taskfailure/classify_test.go
+++ multica/server/pkg/taskfailure/classify_test.go
@@ -75,6 +75,7 @@ func TestClassifyRules(t *testing.T) {
 		{"usage limit", "Account exceeded the daily usage limit", ReasonAgentProviderQuotaLimit},
 		{"hit your limit ascii", "you've hit your limit; upgrade to continue", ReasonAgentProviderQuotaLimit},
 		{"hit your limit curly", "you\u2019ve hit your limit", ReasonAgentProviderQuotaLimit},
+		{"session limit (Claude Code)", "You've hit your session limit \u00b7 resets 8pm (Asia/Shanghai)", ReasonAgentProviderQuotaLimit},
 		{"credits", "Your account has 0 credits remaining", ReasonAgentProviderQuotaLimit},
 		{"quota", "quota exceeded for project foo", ReasonAgentProviderQuotaLimit},
 
