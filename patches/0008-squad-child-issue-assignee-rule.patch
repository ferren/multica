Index: multica/server/internal/handler/squad_briefing.go
===================================================================
--- multica.orig/server/internal/handler/squad_briefing.go
+++ multica/server/internal/handler/squad_briefing.go
@@ -131,7 +131,15 @@ const squadOperatingProtocolHardRules =
   work, the agent runs twice in parallel (once from the mention, once
   from the assignment). Pick exactly one path: either delegate by
   @mention on this issue, or create a ` + "`" + `todo` + "`" + ` child issue assigned to
-  them. Never both for the same work.`
+  them. Never both for the same work.
+- If you pick the child-issue path, you MUST pass ` + "`" + `--assignee <member>` + "`" + `
+  when you run ` + "`" + `multica issue create` + "`" + `. Omitting it does NOT leave the
+  child unassigned — it silently defaults to YOU as the assignee, so the
+  member never receives the work and the child issue just sits there
+  looking delegated when it isn't. If you already created a child issue
+  without an assignee, fix it with
+  ` + "`" + `multica issue update <child-id> --assignee <member>` + "`" + ` rather than
+  leaving it or recreating it.`
 
 // squadOperatingProtocolFor assembles the protocol, selecting the parent-status
 // responsibility that matches this leader's actual authority over the issue.
